#include <stdio.h>

int main(void)
{
    double x = 5.2, y = 2.5;

    double multi = x * y;
    double add = x + y;
    double div = x / y;
    double sub = x - y;

    printf("Multiplication: %f\n", multi);
    return 0;
}